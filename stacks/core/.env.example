# Core stack environment variables — copy to .env and fill in your values

# Timezone
TZ=America/Santiago

# DuckDNS — https://www.duckdns.org
DUCKDNS_SUBDOMAINS=your-subdomain        # e.g. myhomelab (without .duckdns.org)
DUCKDNS_TOKEN=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Volume paths — absolute or relative paths on the Docker host
PORTAINER_DATA=./data/portainer
NPM_DATA=./data/npm/data
NPM_LETSENCRYPT=./data/npm/letsencrypt
DUCKDNS_CONFIG=./data/duckdns/config
HOMEPAGE_CONFIG=./config/homepage

# Homepage — comma-separated list of hostnames/IPs allowed to access the dashboard
# e.g. localhost,192.168.1.100,myhomelab.duckdns.org
HOMEPAGE_ALLOWED_HOSTS=localhost,127.0.0.1

# Homepage service widget credentials (referenced in config/homepage/services.yaml)
HOMEPAGE_VAR_MAC_MINI_IP=192.168.1.x          # Local IP of this Mac Mini
HOMEPAGE_VAR_PORTAINER_API_KEY=               # Portainer → User → Access tokens
HOMEPAGE_VAR_NPM_USER=admin@example.com       # NPM admin email
HOMEPAGE_VAR_NPM_PASS=                        # NPM admin password
HOMEPAGE_VAR_PLEX_TOKEN=                      # https://support.plex.tv/articles/204059436
HOMEPAGE_VAR_JELLYFIN_API_KEY=                # Jellyfin → Dashboard → API keys
