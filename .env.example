# Shared environment variables — copy to .env and fill in your values

# Timezone (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/Santiago

# UID/GID for file permissions inside containers (run `id` to find yours)
PUID=1000
PGID=1000
